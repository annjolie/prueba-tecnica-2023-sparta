import type { InputTextProps } from './types';
export default function InputText({ autoComplete, className, error, help, helpMessage, id, label, name, onBlur, onChange, onFocus, pattern, placeholder, readOnly, required, errorMessage }: InputTextProps): JSX.Element;
